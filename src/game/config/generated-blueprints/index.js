import new_blueprint_editor from "./new_blueprint_editor.js";

// Editor-managed blueprint registry. Registry keys must exactly match blueprint IDs.
export const GENERATED_AREA_BLUEPRINTS = {
  new_blueprint_editor,
};
